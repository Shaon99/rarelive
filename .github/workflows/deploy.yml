on:
  push:
    branches:
      - master

name: "🚀 Deploy website on push"

jobs:
  web-deploy:
    name: "🎉 Deploy"
    runs-on: ubuntu-latest
    steps:
      - name: "🚚 Get latest code"
        uses: actions/checkout@v4

      - name: "📂 Sync files"
        uses: SamKirkland/ftp-deploy-action@v4.3.5
        with:
          server: ftp.dhanshiribd.com
          username: "rareliv@rareliv.optimosell.com"
          password: "8CG%gvw}Xyh9x3PA"
          server-dir: /
          exclude: |
            .git*
            node_modules/**
            vendor/**
            storage/**
            tests/**
            .env
            .github/**
            .vscode/**
            .gitignore
            .idea/**
            .DS_Store
            .env.example
            .env.backup
            .env.production
            .env.staging
            .env.development
            .env.testing
            .env.local
       